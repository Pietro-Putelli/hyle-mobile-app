export interface InputModeTypes {
  Text: string;
  Mic: string;
  Pen: string;
  Scan: string;
  Gallery: string;
}

export const InputModes: InputModeTypes = {
  Text: 'text',
  Mic: 'mic',
  Pen: 'pen',
  Scan: 'scan',
  Gallery: 'gallery',
};
