import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cell: {
    padding: 20,
  },
  headerContent: {
    flexDirection: 'row',
    alignItems: 'flex-end',
    gap: 8,
    flex: 1,
    marginRight: 16,
    marginBottom: 8,
  },
});

export default styles;
