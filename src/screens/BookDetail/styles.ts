import {PICKS_EDITOR_HEIGHT} from '@/components/PicksEditorController/styles';
import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  scrollContainer: {
    flex: 1,
  },
  contextButton: {
    height: 40,
    width: '100%',
    justifyContent: 'center',
    alignItems: 'flex-end',
    paddingRight: 2,
  },
});

export default styles;
